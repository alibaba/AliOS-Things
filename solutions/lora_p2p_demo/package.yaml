name: lora_p2p_demo
version: master
description: demostrate the p2p-comunication over lora radio
type: solution

solution:
  cpu_id: cpu0
  board_name: haas100

depends:
  - cli: master

# supported_boards 中记录当前组件能支持的所有板子
supported_boards:
  - haas100: master

source_file:
  - "*.c"
