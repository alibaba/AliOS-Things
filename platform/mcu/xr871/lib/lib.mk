
NAME := libamr

$(NAME)_TYPE := kernel
$(NAME)_PREBUILT_LIBRARY := libamr.a


NAME := libamren

$(NAME)_TYPE := kernel
$(NAME)_PREBUILT_LIBRARY := libamren.a

NAME := libcedarx

$(NAME)_TYPE := kernel
$(NAME)_PREBUILT_LIBRARY := libcedarx.a

NAME := libmp3

$(NAME)_TYPE := kernel
$(NAME)_PREBUILT_LIBRARY := libmp3.a

NAME := libsc_assistant

$(NAME)_TYPE := kernel
$(NAME)_PREBUILT_LIBRARY := libsc_assistant.a