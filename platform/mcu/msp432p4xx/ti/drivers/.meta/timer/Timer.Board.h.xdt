%%{
    /* get at our module  */
    let Timer     = args[0];

    /* get ti/drivers common utility functions */
    let Common = system.getScript("/ti/drivers/Common.js");

    let instances = Timer.$instances;
    let defs = Common.genBoardHeader(instances, Timer);
%%}
`defs`
