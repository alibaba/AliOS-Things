menuconfig AOS_COMP_OTA
    bool "OTA"
    default y
    help

if AOS_COMP_OTA
config AOS_COMP_OTA_MQTT
    bool "Enable MQTT transport for OTA"
    default y
    help
        select Y to enable MQTT transpor for OTA, or so select CoAP.

if AOS_COMP_OTA_MQTT
source "middleware/uagent/uota/ota_core/Config.in"
source "middleware/uagent/uota/hal/Config.in"
source "middleware/uagent/uota/2nd_boot/Config.in"
source "middleware/uagent/uota/ota_ble/Config.in"
endif
endif
