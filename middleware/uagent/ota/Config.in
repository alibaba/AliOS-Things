config AOS_COMP_OTA
    bool "OTA"
    select AOS_COMP_OTA_CORE
    select AOS_COMP_OTA_HAL
    default n
    help

if AOS_COMP_OTA
config AOS_COMP_OTA_MQTT
    bool "Enable MQTT transport for OTA"
    default y
    help
        select Y to enable MQTT transpor for OTA, or so select CoAP.

if AOS_COMP_OTA_MQTT

source "middleware/uagent/ota/2nd_boot/Config.in"
source "middleware/uagent/ota/hal/Config.in"
source "middleware/uagent/ota/ota_ble/Config.in"
source "middleware/uagent/ota/ota_core/Config.in"

endif

endif
