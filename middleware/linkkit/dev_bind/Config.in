config AOS_COMP_SDK_BIND

    bool "FEATURE_DEV_BIND_ENABLED"
    select INFRA_COMPAT
    select COAP_SERVER
    select AOS_COMP_SDK_MQTT
    select AOS_COMP_SDK_WRAPPERS
    default n
    help
        Enable dev-bind in SDK

if AOS_COMP_SDK_BIND
    config DEV_BIND_ENABLED
        bool
        default y
endif

