config AOS_COMP_USPACE
    bool "user space"
    default n
    help
        set to y if you want to enable user space feature in kernel,
        set to n to disable user space feature.

if AOS_COMP_USPACE
# Configurations for component uspace

endif

