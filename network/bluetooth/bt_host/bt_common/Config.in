menuconfig AOS_COMP_BT_COMMON
    bool "Bluetooth common utilities (queue, timer, fifo, etc.)"
    default n
    help
      AliOS Things NETWORK - Bluetooth common utilities (queue, timer, fifo, etc.)

if AOS_COMP_BT_MESH
# Configurations for comp bt_mesh
endif
