if AOS_CREATE_PROJECT
config AOS_COMP_USB
    bool
    default y
endif

if !AOS_CREATE_PROJECT
config AOS_COMP_USB
    bool "USB Support"
    default n
    help
        usb host library and usb device library support
endif
