
if AOS_CREATE_PROJECT
config AOS_COMP_UMESH2_CORE
    bool "uMesh2 core"
    default y
    help
        AliOS Things uMesh network protocol
endif
if !AOS_CREATE_PROJECT
config AOS_COMP_UMESH2_CORE
    bool "uMesh2 core"
    default n
    help
        AliOS Things uMesh network protocol
endif

if AOS_CREATE_PROJECT
config UMESH_USE_YLOOP
    bool 
    default y
endif

