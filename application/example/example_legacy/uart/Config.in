config AOS_APP_UART
    bool "Uart Sample"
    help
        this is a uart sample form hal uart.h

if AOS_APP_UART
# Configurations for app uart
config SYSINFO_APP_VERSION
    string "Firmware Version"
    default "app-1.0.0-20200214.140831"
    help
        application main firmware version
endif
